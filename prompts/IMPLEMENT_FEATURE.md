# IMPLEMENT FEATURE

Before coding, read AGENTS.md, CURRENT_STATE.md, PROJECT_MAP.md, DECISIONS.md, and the relevant module map.

Implement only the requested feature.

Requirements:
1. Preserve verified legacy behavior.
2. Do not change unrelated modules.
3. Use configuration instead of operational hardcoding.
4. Keep external/network work asynchronous where appropriate.
5. Protect database writes from duplicates and partial failure.
6. Add/update focused tests.
7. Report changed files and reasons.
8. Update CURRENT_STATE.
9. Update DECISIONS only if architecture changed.

If ambiguity affects stored data or architecture, stop and ask.
